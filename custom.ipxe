#!ipxe
###
### netboot.xyz-custom menu
###

:start

:debian-minimal-ssh
set live_endpoint https://scratch.web.garage.mattcen.com/live/
set squash_url ${live_endpoint}filesystem.squashfs
set kernel_url ${live_endpoint}

:boot
imgfree
kernel ${kernel_url}vmlinuz boot=live fetch=${squash_url} initrd=initrd.magic ${cmdline}
initrd ${kernel_url}initrd
boot

:custom_exit
exit 0
